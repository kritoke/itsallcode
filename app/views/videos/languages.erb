<!DOCTYPE html>
<html>
<head>
  <title>it's all code</title>
  <link rel="stylesheet" type="text/css" href="/css/style.min.css">
</head>
<body>
  <div class="wrap">
    <div class="header">
      <div class="nav">
        <% if logged_in? %>
          <p><a href="/logout">Logout</a></p>
          <% else %>
            <p><a href="/login">Login</a> &middot; <a href="/signup">Signup</a></p>
        <% end %>
      </div>
      <h1>it's all code - <%= @language.upcase %></h1>
    </div>
    <% @videos.each do |video| %>
      <section>
        <div class="video_thumb">
          <a href="/<%= video.language %>/<%= video.id %>"><img src="<%= video.thumbnail_url %>"></a>
        </div>
        <div class="video_info">
          <a href="/<%= video.language %>/<%= video.id %>"><%= video.title.first(35) %></a>
          <% if logged_in? %>
            (<a href="/<%= video.language %>/<%= video.id %>/edit">edit</a>)
            <% end %>
          <br />
          Language: <a href="/<%= video.language %>"><%= video.language %></a><br />
          Published on <%= video.published.in_time_zone('America/Chicago').to_datetime.strftime("%B %d, %Y at %l:%M %p Central") %><br />
          Views: <%= video.view_count %> <br />
        </div>
      </section>
    <% end %>

  </div>
</body>
</html>
